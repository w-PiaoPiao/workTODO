# 悬浮待办事项软件 — 执行步骤

> 版本：v0.4.4 | 最后更新：2026-08-14

## 开发策略

**原则**：小步快跑，每步可验证。绝不一口气写大量代码，每完成一步都测试确认。

## 阶段 1：基础设施 ✅

### 步骤 1.1：项目初始化 ✅ (2026-06-29)
- [x] 安装 PySide6、appdirs
- [x] 创建项目目录结构
- [x] 编写需求文档、技术方案、设计规范、开发规范、执行步骤
- [x] 初始化 CLAUDE.md 项目指引
- [x] 初始化 Git 仓库

### 步骤 1.2：实现数据模型层 ✅ (2026-06-29)
- [x] 实现 `todo_item.py` — TodoItem、ProgressEntry 数据类（序列化/反序列化、时间友好显示）
- [x] 实现 `todo_store.py` — JSON 原子读写、损坏文件自动备份、CRUD、归档管理
- [x] 验证：9 项测试全部通过（创建、查询、更新、删除、归档、恢复、损坏恢复）

### 步骤 1.3：实现主题系统 ✅ (2026-06-29)
- [x] 实现 `theme.py` — 颜色方案（Win11 Fluent 风格）、字体（YaHei UI）、完整 QSS 样式表

## 阶段 2：核心 UI 组件 ✅

### 步骤 2.1：主窗口框架 ✅
- [x] 实现 `main_window.py` — 无边框、 `WindowStaysOnTopHint` 置顶、鼠标拖拽
- [x] QStackedWidget 管理折叠/展开视图切换
- [x] QPropertyAnimation 实现 200ms 平滑动画
- [x] 屏幕边缘吸附

### 步骤 2.2：折叠/展开模式 ✅
- [x] 实现 `collapsed_view.py` — 紧凑条（图标 + 计数 + 快速添加按钮 + 展开按钮）
- [x] 实现 `expanded_view.py` — 完整列表（标题栏、搜索、快速添加、滚动列表、归档入口）
- [x] 空状态 / 搜索结果为空状态
- [x] 验证：双击切换模式，动画流畅

### 步骤 2.3：待办卡片组件 ✅
- [x] 实现 `todo_card.py` — 复选框办结、标题显示、办结/删除按钮
- [x] 双击标题内联编辑、进度输入
- [x] 实现 `progress_widget.py` — 进度条目折叠/展开显示
- [x] 实现 `archive_dialog.py` — 归档查看、搜索、恢复功能
- [x] 验证：全部 UI 组件模块导入成功

## 阶段 3：控制器与完整功能 ✅

### 步骤 3.1：核心业务逻辑 ✅
- [x] 实现 `app_controller.py` — 信号连接、业务逻辑
- [x] 快速添加 → 添加进度 → 办结归档 → 恢复
- [x] 搜索过滤（300ms 防抖）
- [x] 系统托盘（最小化/恢复/退出）
- [x] 删除确认对话框
- [x] 错误处理和通知
- [x] 验证：4 条测试数据端到端运行正常，GUI 启动成功

### 步骤 3.2：增强功能
- [x] 窗口阴影美化（QGraphicsDropShadowEffect）
- [x] 位置持久化（保存/恢复窗口位置）
- [x] 置顶切换（折叠/展开视图 📌 按钮）
- [x] 键盘快捷键（Ctrl+N 新建、Ctrl+F 搜索、Esc 折叠）
- [x] 自动归档旧项目（30天）

## 阶段 4：打包 ✅

### 步骤 4.1：打包 EXE ✅ (2026-06-30)
- [x] PyInstaller 配置（--onefile --windowed）
- [x] 打包测试（onedir + onefile 均通过）
- [x] 验证：EXE 启动正常（6 秒无报错，exit=124 超时即正常退出）
- [x] 清理构建中间产物
- [ ] 注：EXE 位于 `dist/待办事项和便签.exe`（46MB，含 PySide6 完整运行时）

## 阶段 5：v0.1.1 功能增强

### 步骤 5.1：进度条只显示最新一条 ✅ (2026-06-30)
- [x] 重写 `ProgressWidget`：始终只显示最新一条进度
- [x] 超过 1 条时显示"查看全部 N 条进度 ▼"可点击标签
- [x] 点击展开显示全部、点击外部收起
- [x] ExpandedView 事件过滤器检测外部点击

### 步骤 5.2：卡片拖动排序 ✅ (2026-06-30)
- [x] TodoItem 添加 position 字段支持手动排序
- [x] TodoCard 鼠标拖拽（QDrag）+ 半透明反馈
- [x] ExpandedView 拖放处理（Drop 指示线、自动滚动）
- [x] TodoStore.reorder_items 持久化排序结果

### 步骤 5.3：置顶箭头过渡动画 ✅ (2026-06-30)
- [x] 点击 ↑ 时卡片从当前位置动画移动到顶部
- [x] 幽灵覆盖层 + QPropertyAnimation 实现平滑过渡

### 步骤 5.4：更新版本与文档 ✅ (2026-06-30)
- [x] 版本号升级到 0.1.1
- [x] 更新执行步骤
- [x] 更新开发日志

## 阶段 6：代码质量与架构优化 ✅ (v0.3.1，2026-07-22)

### 步骤 6.1：通用按钮样式集中管理 ✅
- [x] AppTheme 新增 10 个工厂方法（icon_btn、toggle_btn、outline_btn 等）
- [x] 消除 5 个视图文件中散落的重复 QSS 字符串
- [x] 主题切换观察者机制（AppTheme.register），视图自动注册监听

### 步骤 6.2：搜索职责上移到 Controller ✅
- [x] 控制器维护 `_search_query`，在 `_refresh_views()` 中保持搜索状态
- [x] 视图 `refresh(items, search_query)` 不再负责过滤逻辑

### 步骤 6.3：单实例锁改进 ✅
- [x] QSharedMemory → QLockFile，进程崩溃后自动释放，无残留

### 步骤 6.4：ExpandedView 拆分解耦 ✅
- [x] 提取 `TitleBar` 独立组件（`app/views/title_bar.py`）
- [x] 提取 `DragDropManager` 独立模块（`app/views/drag_drop_manager.py`）
- [x] ExpandedView 从 1,047 行降至 696 行

### 步骤 6.5：差异化刷新 ✅
- [x] `refresh()` 改为维护 `_card_map` 映射表，按需增删改单张卡片
- [x] 非排序操作不再销毁重建全部卡片

### 步骤 6.6：其他代码优化 ✅
- [x] `_progress_expanded_card` 改用 item_id 索引，消除悬挂引用
- [x] `ClickableElidedLabel` 事件改为选择性拦截，保留右键功能
- [x] 模型层全覆盖测试（33 项全部通过）

### 步骤 6.7：文档同步 ✅
- [x] 更新执行步骤
- [x] 创建 todolist 开发日志

## 阶段 7：v0.4.0 功能增强与优化 ✅ (2026-07-31)

### 步骤 7.1：性能优化
- [x] TodoStore 内存缓存：写操作只标记脏，控制器 500ms 防抖落盘，退出时强制 flush
- [x] 透明度持久化改为滑块松手时写入，消除拖动时高频写注册表

### 步骤 7.2：截止日期与提醒
- [x] TodoItem 新增 due_date 字段，卡片 📅 按钮设置/清除（YYYY-MM-DD）
- [x] 截止日期徽标：今天到期 / 明天到期 / 已过期（红色）
- [x] 启动时 + 每小时检查到期提醒（托盘通知，每天每项只提醒一次）

### 步骤 7.3：分类标签
- [x] TodoItem 新增 tags 字段，添加时自动提取标题中的 #标签
- [x] 卡片显示标签 chips，标题编辑后重新提取
- [x] 列表顶部标签筛选行（全部 / #标签）

### 步骤 7.4：便签功能（彩色便利贴）
- [x] 新增 Note 模型 + NoteStore（notes.json）
- [x] 展开视图待办/便签 Tab 切换
- [x] NoteView 便签列表 + NoteDialog 编辑（多行文本 + 5 色选择）

### 步骤 7.5：数据备份与统计
- [x] TodoStore.export_all / import_all：导出全部数据为单个 JSON，导入替换（含格式校验）
- [x] 页脚 💾 备份按钮（导出/导入菜单）
- [x] 标题栏 📊 统计面板（待办数、今日/本周/累计完成、归档数）

### 步骤 7.6：主题与字号
- [x] 主题三态轮换：浅色 → 深色 → 跟随系统（30 秒轮询注册表）
- [x] 字号缩放（0.85~1.3），⚙️ 设置面板：透明度 + 字号

### 步骤 7.7：代码与文档清理
- [x] 修复 expanded_view 重复 setLayout、无效 f-string、CST 时区统一
- [x] ArchiveDialog 样式集中到 AppTheme（对话框样式工厂）
- [x] 新增 50 项测试（模型新字段、Store 缓存/flush/导出导入、NoteStore、控制器级 offscreen 测试），共 83 项全部通过
- [x] 应用图标 icon.ico（tools/make_icon.py 生成多尺寸），main.py 设置窗口图标，打包命令加 --icon
- [x] 文档同步（技术方案、需求文档、README、CLAUDE.md 打包约定）

## 阶段 8：v0.4.1 桌宠形态 ✅ (2026-08-12)

### 步骤 8.1：PetView 替换折叠条 ✅
- [x] 新建 `pet_view.py`：透明窗口桌宠形象，替代原 320×48 紧凑条（collapsed_view.py 已删除）
- [x] 主窗口 WA_TranslucentBackground，折叠尺寸 320×48 → 140×140
- [x] 单击展开、右上角计数角标（随主题变色）、右键菜单（展开/快速添加/置顶/退出）
- [x] 设置面板新增"宠物"形象切换（缩略图横排，QSettings 持久化）
- [x] 素材双目录扫描：内置 resources/pets + 用户 data/pets（用户目录优先，打包后可增补）
- [x] 默认 emoji 兜底形象（tools/make_pet_placeholders.py 生成 🐱🐶🐰🐼）

### 步骤 8.2：拖拽修复 ✅
- [x] 修复事件转发目标错误：parentWidget()（QStackedWidget）→ window()（MainWindow）
- [x] 回归测试：拖拽移动窗口、单击不移动只展开

### 步骤 8.3：白底自动去除 ✅
- [x] ensure_transparent()：四边种子点 floodfill 标记连通边缘白色 → 透明（内部白色保留）
- [x] 处理结果缓存 data/pets/_processed/（mtime 过期自动重建）
- [x] 已有透明的 PNG 直接使用原图，GIF 暂不处理

### 步骤 8.4：动画丰富 ✅
- [x] PetCanvas 自绘重构：offsetY/scale/angle Qt 属性，paintEvent 统一变换
- [x] 漂浮/呼吸保留并修复段间跳变（start 衔接前段 end）
- [x] 随机小动作：每 6~14s 随机歪头（±8°）或跳跃（12px），与漂浮互斥防属性争抢
- [x] 悬停弹跳反馈（1.0→1.12→1.0），与呼吸互斥
- [x] 动画生命周期：贴顶/拖拽/展开时停止，恢复时重启
- [x] 15 项桌宠测试全部通过，全量 123 项通过

### 步骤 8.5：锚点式展开（消除瞬移） ✅
- [x] 展开前按桌宠位置选择锚点：默认左上（向右下展开）；右侧受限→右上锚（向左下）；下方受限→左下锚（向右上）；双侧受限→右下锚（向左上）
- [x] `_animate_size` 支持锚点，基准矩形在改尺寸约束前记录（setMinimumSize 触发中间态 resize 会错位）
- [x] 折叠用同一锚点反向收缩 → 展开/折叠可逆，桌宠回到原位，全程无瞬移

### 步骤 8.6：暂停动画 + 素材清理 ✅
- [x] 桌宠右键菜单新增"暂停动画"（checkable），QSettings `window/pet_animation` 持久化，恢复时若处折叠态立即重启
- [x] 删除内置 emoji 默认形象（cat/dog/rabbit/panda）与 make_pet_placeholders.py，仅保留用户素材 deepseek娘.jpg
- [x] load_pet 兜底逻辑简化：素材缺失回退第一个可用；素材全空不崩溃
- [x] 全量 130 项测试通过

## 阶段 9：代码审查修复 ✅ (v0.4.2，2026-08-13)

### 步骤 9.1：修复备份菜单崩溃 ✅
- [x] `app_controller.py` 补 `QMenu` / `QAction` 导入（`_on_backup_clicked` 之前抛 NameError）
- [x] 补 `TestControllerBackup` 3 项测试（菜单不崩溃 / 导出 / 导入），fake 类规避模态阻塞

### 步骤 9.2：去重重构 ✅
- [x] 新建 `app/views/ui_utils.py`：`DragMixin`（拖拽） + `clear_layout`（清空布局）
- [x] `note.py` 删除重复 `_now_iso`，改从 `todo_item` 导入
- [x] `note_dialog.py` / `archive_dialog.py` 继承 `DragMixin` 删除重复拖拽
- [x] `expanded_view` / `note_view` / `archive_dialog` 统一用 `clear_layout`

### 步骤 9.3：体验与健壮性 ✅
- [x] `main.py` 单实例失败时 QMessageBox 提示；修正高 DPI 误导注释
- [x] 设置面板改 `setFixedWidth` + `adjustSize` 自适应高度
- [x] `reminder_service` 清理历史 `reminders/*` QSettings 键
- [x] `note_view` 新建 `NoteCard` 子类替换动态方法 hack
- [x] `archive_item` 幂等防御 + `restore_item` 恢复时重排 position

### 步骤 9.4：文档同步 ✅
- [x] 版本号 0.4.1 → 0.4.2（`config.py` + README）
- [x] 全量 133 项测试通过

## 阶段 10：展开界面 UI 优化 ✅ (v0.4.3，2026-08-13)

### 步骤 10.1：SVG 图标体系 ✅
- [x] 新建 `app/resources/icons/*.svg`（21 个 Fluent 单色图标，`#000000` 占位色）
- [x] 新建 `app/views/icons.py`：`AppIcons` 读 SVG → 替换占位色 → `QSvgRenderer` 渲染，含 Normal/Active 双态（hover 变 accent），`(名称,尺寸,颜色)` 缓存
- [x] `AppTheme.switch_theme` 调用 `AppIcons.clear()` 释放缓存

### 步骤 10.2：标题栏精简 + 溢出菜单 ✅
- [x] `title_bar.py` 8 按钮 → 标题 + 搜索 + 设置 + 折叠 + 「⋯」
- [x] 「⋯」QMenu 收纳：置顶/收起全部卡片/统计/主题/开机自启/备份/退出
- [x] `ThemeService` 新增 `set_mode(mode)`，主题三选一（`QActionGroup` 单选）
- [x] 统计面板锚定改 `more_btn_global_rect`

### 步骤 10.3：卡片优化 ✅
- [x] 日期/置顶/删除按钮 hover 显示（`enterEvent`/`leaveEvent`），办结常驻
- [x] 置顶卡片左侧 3px accent 竖条（`card_style(sticky=)`）

### 步骤 10.4：布局与页脚 ✅
- [x] 搜索框独立成行（含关闭按钮），不再挤压添加栏
- [x] 页脚删除备份/退出按钮（移入菜单），仅留归档 + 计数 + 缩放手柄

### 步骤 10.5：细节体验 ✅
- [x] 标签筛选 chips 横向滚动（QScrollArea）
- [x] 进度时间列 `setFixedWidth(50)` → `setMinimumWidth(50)`
- [x] 空状态加「＋ 立即添加」按钮

### 步骤 10.6：文档同步 ✅
- [x] 版本号 0.4.2 → 0.4.3（`config.py`）
- [x] 新增 `tests/test_ui_refactor.py` 11 项测试
- [x] 全量 144 项测试通过

## 阶段 11：全面优化修复 ✅ (v0.4.4，2026-08-14)

### 步骤 11.1：Bug 修复 ✅
- [x] 搜索索引失效修复：`_on_add_item` 补 `_invalidate_search_index()`，搜索状态下新增待办立即可见
- [x] 标题内联编辑改变 #标签 后，标签筛选行同步刷新（`_on_title_changed`）
- [x] 归档搜索覆盖进度文本（与主搜索一致）
- [x] 统计口径修正：`total_completed` 只计有完成时间的归档（自动归档的未完成项不计入）

### 步骤 11.2：数据安全 ✅
- [x] 备份导出/导入包含便签（`export_all(notes=)` / `import_all` 返回 notes；旧格式备份不覆盖现有便签）
- [x] 导入前自动快照到 `data/backups/`（保留最近 7 份，`AppConfig.backup_dir()`）
- [x] 自动归档托盘提示（每天最多一次，`notify/auto_archive_date` 键去重）

### 步骤 11.3：健壮性与代码质量 ✅
- [x] `json_io.load_json_list` 抽取，TodoStore/NoteStore 加载去重
- [x] ReminderService `supports_messages` 下沉（定时器路径也受保护）
- [x] 清理 main.py/控制器重复 QSS+palette 设置；main.py 复用 `AppConfig.resource_path`
- [x] 死代码清理：`COLLAPSED_WIDTH/HEIGHT`、`MAX_PROGRESS_COLLAPSED`、`DUE_REMIND_HOUR`、`pin_btn_style`、`danger_fill_btn`、`add_entry`、`fullText`
- [x] QSettings 收敛为 `AppConfig.settings()`（15 处替换 + 测试 fixture 适配）
- [x] `note_dialog` 颜色行引用替换魔法索引；TodoCard 事件过滤器 try/finally + RuntimeError 防御

### 步骤 11.4：工具链与打包 ✅
- [x] `requirements.txt` 补 Pillow；新增 `requirements-dev.txt`
- [x] 新增 `pyproject.toml`（ruff）+ `.pre-commit-config.yaml` + `.gitignore` 补充
- [x] 新增 `tools/build.ps1` 一键打包（排除未用 Qt 模块，产物更小），CLAUDE.md/README 同步

### 步骤 11.5：测试与文档同步 ✅
- [x] 新增测试：搜索索引、标签筛选刷新、便签备份 round-trip、旧格式兼容、统计口径、归档搜索、提醒 supports_messages、自动归档通知去重
- [x] 版本号 0.4.3 → 0.4.4（`config.py`）
- [x] 更新 README / 技术方案 / CLAUDE.md / 开发日志

### 步骤 11.6：主题底色反转修复 ✅
- [x] 根因：QSS 激活时 Qt 按"派生调色板"为控件计算背景色，且**不跟随 `AppTheme.apply_palette` 的应用级调色板**；列表容器/视口因此渲染默认色或**陈旧主题色**（深色模式下列表区显示浅色、浅色模式显示深色）
- [x] 修复：列表区所有可见层显式指定主题化 QSS 背景并在 `reapply_theme` 刷新——
  - `ExpandedView`：滚动区框架 + 视口（objectName ID 选择器）显式 `bg_primary`
  - `NoteView`：根 + 视口显式 `bg_primary`
  - `ArchiveDialog`：视口显式 `bg_card`（对话框底色）
  - 列表容器统一 `background: transparent` + 关闭 autoFill，让主题化背景透出
- [x] 验证：offscreen 渲染对比（修复前浅色→#1e1e1e/深色→#f3f3f3 反转；修复后浅色→#f3f3f3/深色→#1e1e1e，便签页/归档对话框同理）
