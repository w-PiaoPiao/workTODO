# 开发日志 — 2026-06-29（第三轮）

## 本次完成

### 5 项需求实现
1. **窗口缩放** — QSizeGrip 在展开视图页脚右下角，min 300×200 / max 1200×1080
2. **关闭按钮移到底部** — 标题栏移除 ✕，页脚右侧加「✕ 退出」
3. **去复选框** — TodoCard 删除 _check_btn，仅保留办结按钮
4. **待办置顶** — TodoItem 增加 sticky 字段，↑ 按钮，排序优先
5. **文本溢出** — ⚠️ 未完全修复

### Bug 修复
- 置顶切换窗口消失 → QWindow.setFlag() 消除闪烁
- UpdateLayeredWindowIndirect 报错 → 移除阴影
- 托盘图标缺失 → 手绘蓝色图标
- 置顶按钮颜色 → 📌红/📍灰 + 文字切换
- QSizeGrip 被遮挡 → 移入展开视图页脚
- 待办置顶双重翻转 → 卡片只发信号，控制器处理
- 自动归档条件 → 创建超30天 + 最后进度也超30天

### 需继续
- ⚠️ **文本溢出未修复**（编号 5）：标签仍可能溢出卡片宽度，
  elidedText + setMaximumWidth 方案在 layout 完成前调用时机有问题。
  明天需要改用 QLabel 子类或 showEvent 后延迟触发。

## Git 历史
```
ae0c5fa [feat] 5项更新：缩放、关闭按钮底部、去复选框、待办置顶、溢出修复
ef8e4eb [fix] 缩放柄显示 + 文本省略号截断
d3e41ec [fix] QSizeGrip进页脚 + 文本省略 + 置顶去掉双重翻转
a715d88 [fix] QSizeGrip三角图案 + 文本省略物理约束宽度 + tooltip亮底 + 最大高1080
```
